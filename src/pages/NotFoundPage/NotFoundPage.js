import css from './NotFoundPage.module.css'

const NotFoundPage = () => {

  return (
    
    <div className={css.NotFoundContainer}>
        <p className={css.NotFoundText}>Not Found Page</p>
    </div>
  );
};

export default NotFoundPage